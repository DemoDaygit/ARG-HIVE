import { QuizQuestion } from './quiz';

/**
 * Quiz card data — populated by the content pipeline (see docs/ENGINE.md).
 * Placeholder while authored content is being integrated.
 */
export const QUIZZES: QuizQuestion[] = [];
