# Безопасный деплой без раскрытия API ключа

**ВАЖНО:** Никогда не храните API ключи в клиентском коде или в Git репозитории!

Этот гайд показывает, как безопасно развернуть приложение, используя серверные proxy функции для защиты вашего Gemini API ключа.

---

## 🔒 Проблема

В обычном React приложении API ключ встраивается в frontend код:
```typescript
// ❌ НЕБЕЗОПАСНО - ключ виден всем!
const apiKey = "AIzaSy...";
fetch(`https://api.google.com?key=${apiKey}`);
```

**Последствия:**
- Любой может украсть ваш ключ через DevTools
- Злоумышленники могут использовать ваш ключ
- Вы платите за чужое использование API

---

## ✅ Решение: Серверный Proxy

Мы создали серверный proxy, который:
1. Хранит API ключ на сервере (безопасно)
2. Клиент отправляет запросы на proxy
3. Proxy передаёт запросы в Gemini API
4. API ключ никогда не попадает в браузер

```
Браузер → Proxy Сервер → Gemini API
         (с API ключом)
```

---

## 📦 Варианты деплоя

### 1️⃣ Vercel (Рекомендуется)

Vercel автоматически поддерживает serverless functions.

#### Шаг 1: Установите API ключ в Vercel

1. Разверните на Vercel:
```bash
vercel
```

2. Перейдите в Dashboard → Settings → Environment Variables

3. Добавьте переменную:
   - **Name:** `GEMINI_API_KEY`
   - **Value:** ваш Gemini API ключ
   - **Environment:** Production, Preview, Development

4. Переразверните:
```bash
vercel --prod
```

#### Шаг 2: Готово!

Serverless function автоматически доступна по адресу:
```
https://ваш-домен.vercel.app/api/gemini
```

#### Как это работает:

Файл `/api/gemini.js` автоматически становится serverless function:
- ✅ API ключ в переменных окружения Vercel (безопасно)
- ✅ Автоматический HTTPS
- ✅ Автоматическое масштабирование
- ✅ Бесплатный tier для малых проектов

---

### 2️⃣ Netlify

Netlify тоже поддерживает serverless functions.

#### Шаг 1: Установите API ключ в Netlify

1. Разверните на Netlify:
```bash
netlify deploy --prod
```

2. Перейдите в Site settings → Environment variables

3. Добавьте переменную:
   - **Key:** `GEMINI_API_KEY`
   - **Value:** ваш Gemini API ключ

4. Переразверните сайт

#### Шаг 2: Готово!

Function автоматически доступна по адресу:
```
https://ваш-сайт.netlify.app/.netlify/functions/gemini
```

#### Как это работает:

Файл `/netlify/functions/gemini.js` становится serverless function:
- ✅ API ключ в переменных окружения (безопасно)
- ✅ Автоматический HTTPS
- ✅ Автоматическое масштабирование

---

### 3️⃣ Docker с Backend

Для самостоятельного хостинга (VPS, AWS, GCP и т.д.)

#### Шаг 1: Настройте .env.production

```bash
# .env.production
GEMINI_API_KEY=ваш_реальный_ключ_здесь
PORT=3000
NODE_ENV=production
```

**⚠️ ВАЖНО:** Добавьте `.env.production` в `.gitignore`!

#### Шаг 2: Запустите с Docker Compose

```bash
# Вариант A: Используя docker-compose.fullstack.yml
docker-compose -f docker-compose.fullstack.yml up -d

# Вариант B: Используя обычный docker-compose с .env файлом
GEMINI_API_KEY=your_key docker-compose up -d
```

#### Шаг 3: Готово!

Приложение запустится на:
```
http://localhost:3000
```

Backend API доступен по:
```
http://localhost:3000/api/gemini
```

#### Как это работает:

- Express сервер (`server/index.js`) обслуживает и frontend и API
- API ключ читается из переменных окружения
- ✅ API ключ хранится на сервере (безопасно)
- ✅ Frontend и Backend в одном контейнере

---

## 🛠️ Использование в коде

### Простой способ - используйте готовую утилиту:

```typescript
import { geminiClient } from '@/utils/geminiClient';

// Автоматически использует правильный endpoint!
const response = await geminiClient.generate({
  prompt: "Привет, расскажи шутку",
  model: "gemini-pro" // опционально
});

console.log(response);
```

### Ручной способ:

```typescript
// Отправляем запрос на proxy endpoint
const response = await fetch('/api/gemini', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    prompt: "Привет, расскажи шутку",
    model: "gemini-pro"
  }),
});

const data = await response.json();
console.log(data.candidates[0].content.parts[0].text);
```

---

## 🔍 Проверка безопасности

### Как убедиться, что ключ защищён:

1. **Откройте DevTools → Network**
2. **Выполните запрос к API**
3. **Проверьте запросы:**
   - ✅ Запросы идут на `/api/gemini` (ваш домен)
   - ❌ НЕ на `generativelanguage.googleapis.com`
   - ✅ В запросах НЕТ `?key=` параметра

4. **Проверьте исходный код:**
   - ✅ Поиск по "AIza" ничего не находит
   - ✅ `GEMINI_API_KEY` нигде не используется в браузерном коде

---

## 📊 Сравнение методов

| Метод | Безопасность | Сложность | Стоимость | Масштабируемость |
|-------|-------------|-----------|-----------|------------------|
| **Vercel** | ✅ Высокая | 🟢 Простая | 🟢 Free tier | ✅ Отличная |
| **Netlify** | ✅ Высокая | 🟢 Простая | 🟢 Free tier | ✅ Отличная |
| **Docker** | ✅ Высокая | 🟡 Средняя | 🟡 VPS стоимость | 🟡 Требует настройки |
| **Клиент** | ❌ **НЕТ!** | 🟢 Простая | 🔴 Риск краж | ❌ Нет |

---

## 🚀 Быстрый старт

### Vercel (30 секунд):
```bash
# 1. Деплой
vercel --prod

# 2. Установите GEMINI_API_KEY в Vercel Dashboard

# 3. Готово!
```

### Netlify (1 минута):
```bash
# 1. Деплой
netlify deploy --prod

# 2. Установите GEMINI_API_KEY в Netlify Settings

# 3. Готово!
```

### Docker (2 минуты):
```bash
# 1. Создайте .env.production
echo "GEMINI_API_KEY=your_key" > .env.production

# 2. Запустите
docker-compose -f docker-compose.fullstack.yml up -d

# 3. Готово!
```

---

## 🔐 Дополнительные меры безопасности

### 1. Rate Limiting

Добавьте ограничение запросов:

```typescript
// server/index.js
const rateLimit = require('express-rate-limit');

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 минут
  max: 100 // максимум 100 запросов
});

app.use('/api/gemini', limiter);
```

### 2. CORS ограничения

Разрешите только ваш домен:

```typescript
// server/index.js
const cors = require('cors');

app.use(cors({
  origin: 'https://yourdomain.com'
}));
```

### 3. API Key Rotation

Регулярно обновляйте API ключи:
1. Создайте новый ключ в Google AI Studio
2. Обновите переменную окружения
3. Удалите старый ключ

---

## ❓ Частые вопросы

### Q: Можно ли использовать без backend?
**A:** Нет, если вы хотите безопасность. API ключ всегда должен быть на сервере.

### Q: Замедлит ли proxy мою app?
**A:** Минимально (~50-100мс). Serverless functions очень быстрые.

### Q: Будет ли это дороже?
**A:** Нет. Vercel/Netlify имеют generous free tiers для hobby проектов.

### Q: Что если я хочу использовать свой домен?
**A:** Все платформы поддерживают custom domains бесплатно!

---

## 📚 Ресурсы

- [Vercel Serverless Functions](https://vercel.com/docs/functions)
- [Netlify Functions](https://docs.netlify.com/functions/overview/)
- [Google Gemini API](https://ai.google.dev/docs)
- [API Security Best Practices](https://owasp.org/www-project-api-security/)

---

## ✅ Checklist перед деплоем

- [ ] `.env.production` добавлен в `.gitignore`
- [ ] API ключ установлен в переменные окружения платформы
- [ ] Проверили, что ключ не встречается в исходном коде
- [ ] Протестировали proxy endpoint локально
- [ ] Настроили CORS (если нужно)
- [ ] Настроили rate limiting (рекомендуется)
- [ ] Удалили все console.log с чувствительной информацией

---

**Готово!** 🎉 Теперь ваш API ключ в безопасности!
